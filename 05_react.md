# React

## React Query

libary for managing remote (server) state

data is stored in a cache

React fetcht Daten von API (server) in den Cache -> Automatic re-fetching z.B. nach t= x nach raus aus dem Browser und zurück

von da an arbeiten mit Komponenten mit den Daten aus dem Cache

-> Schnell & responsiv

automaitc loading & error state

ACHTUNG: remote state ist FUNDAMENTALLY unterschiedlich zum regular (ui) state

